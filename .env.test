# Test Environment Configuration for Mobile PWA Testing
NODE_ENV=development
DATABASE_URL=postgresql://test:test@localhost:5432/custodial_test
PORT=5000

# Note: This is a test configuration for local mobile testing
# The database will be mocked or can use a local PostgreSQL instance