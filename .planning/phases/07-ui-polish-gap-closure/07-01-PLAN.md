---
phase: 07-ui-polish-gap-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/quick-capture.tsx
autonomous: true
must_haves:
  truths:
    - All secondary interactive controls have minimum 44px touch target
    - Location preset buttons in Quick Capture meet MOB-01 requirement
    - No UI controls below 44px minimum remain in affected files
  artifacts:
    - path: src/pages/quick-capture.tsx
      provides: Updated touch targets for location presets and secondary controls
  key_links:
    - from: Tailwind CSS classes
      to: Touch target sizes
      via: min-h-[44px] and min-w-[44px] classes
---

<objective>
Fix touch target violations in Quick Capture to meet MOB-01 requirement (44px minimum for secondary controls).
</objective>

<context>
**Gap from Phase 04 Verification:**
- File: `src/pages/quick-capture.tsx:356`
- Issue: Location preset buttons use `min-h-[40px]` which is below 44px requirement
- Requirement: MOB-01 - Large touch targets for field use (min 44px)

**Current State:**
- Primary capture button is already 64px (compliant)
- Secondary location presets are 40px (non-compliant)
- Need to update to 44px minimum
</context>

<implementation>
**Changes Required:**
1. Update location preset buttons from `min-h-[40px]` to `min-h-[44px]`
2. Ensure minimum width also meets 44px where applicable
3. Verify no other secondary controls in quick-capture.tsx are below minimum

**Target Lines:**
- Around line 356: Location preset buttons
</implementation>

<tasks>

<task name="Update location preset touch targets to 44px" type="auto">
<description>Change min-h-[40px] to min-h-[44px] for location preset buttons</description>
<files>
- src/pages/quick-capture.tsx
</files>
<action>
1. Read quick-capture.tsx to locate location preset buttons
2. Find all instances of `min-h-[40px]` for secondary controls
3. Update to `min-h-[44px]`
4. Add `min-w-[44px]` where missing for touch targets
5. Save file
</action>
<verify>
- All location preset buttons use min-h-[44px]
- TypeScript compilation passes (`npm run check`)
- No build errors
</verify>
<done>Touch targets updated to meet MOB-01 requirement</done>
</task>

<task name="Verify all secondary controls meet minimum" type="auto">
<description>Audit quick-capture.tsx for any other controls below 44px</description>
<action>
1. Search for all min-h classes in quick-capture.tsx
2. Verify none are below [44px] for interactive elements
3. Check button and input sizes
</action>
<verify>
- No interactive controls below 44px minimum
- All secondary buttons updated
- All inputs have adequate touch targets
</verify>
<done>Full audit confirms MOB-01 compliance</done>
</task>

</tasks>

<verification>
- Location preset buttons use min-h-[44px]
- All secondary controls meet 44px minimum
- Quick-capture.tsx passes visual inspection
</verification>

<success_criteria>
- [ ] Location preset buttons updated to 44px touch target
- [ ] All secondary controls in quick-capture.tsx meet minimum
- [ ] TypeScript compilation passes
- [ ] Commit includes clear description of MOB-01 fix
</success_criteria>
