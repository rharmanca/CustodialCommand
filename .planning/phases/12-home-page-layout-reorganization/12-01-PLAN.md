---
phase: 12-home-page-layout-reorganization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/Dashboard.tsx
  - src/components/dashboard/QuickCaptureCard.tsx
autonomous: true
must_haves:
  truths:
    - Dashboard has distinct workflow sections (Capture, Review, Analyze)
    - Quick Capture is the most prominent element in the bottom 1/3 of the screen
    - Quick Capture displays icon + label + brief description per user decision
    - Background color blocks visually separate workflow sections
    - Primary actions are always visible (not collapsed)
  artifacts:
    - path: src/components/dashboard/QuickCaptureCard.tsx
      provides: Reusable Quick Capture card with icon, label, description
      exports: [QuickCaptureCard]
    - path: src/pages/Dashboard.tsx
      provides: Workflow section layout with background color blocks
      contains: [WorkflowSection component, QuickCaptureCard usage]
  key_links:
    - from: src/pages/Dashboard.tsx
      to: src/components/dashboard/QuickCaptureCard.tsx
      via: import statement
      pattern: import.*QuickCaptureCard
---

<objective>
Create dashboard structure with workflow sections and prominent Quick Capture card positioned in thumb zone (bottom 1/3).

Purpose: Satisfy LAYOUT-01 requirement - Quick Capture as primary, most prominent CTA.
Output: Updated Dashboard.tsx with workflow sections and new QuickCaptureCard component.
</objective>

<execution_context>
@C:/Users/veloc/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/veloc/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-home-page-layout-reorganization/12-CONTEXT.md
@.planning/phases/12-home-page-layout-reorganization/12-RESEARCH.md
@.planning/ROADMAP.md

# Existing dashboard for reference
@src/pages/Dashboard.tsx

# Existing components to reuse patterns
@src/components/ui/card.tsx
@src/components/ui/button.tsx
</context>

<tasks>

<task type="auto">
  <name>Create QuickCaptureCard component</name>
  <files>src/components/dashboard/QuickCaptureCard.tsx</files>
  <action>
Create a new QuickCaptureCard component that displays:
1. Large camera icon (48px) in amber/warm color (#F59E0B)
2. Bold label "Quick Capture" (text-lg font-semibold)
3. Brief description "Capture issues while walking" (text-sm text-muted-foreground)
4. Large touch target (minimum 120px height, full width on mobile)
5. Hover/active states for desktop interaction
6. onClick prop to trigger navigation to quick capture

Use shadcn Card component as base. Apply amber background tint (bg-amber-50) with amber border (border-amber-200). Add subtle shadow for prominence. Ensure entire card is clickable with cursor-pointer.

Component signature:
```typescript
interface QuickCaptureCardProps {
  onClick: () => void;
  className?: string;
}
```
  </action>
  <verify>cat src/components/dashboard/QuickCaptureCard.tsx | head -50</verify>
  <done>Component exists with Camera icon, label, description, amber styling, and onClick handler. File passes TypeScript check.</done>
</task>

<task type="auto">
  <name>Restructure Dashboard with workflow sections</name>
  <files>src/pages/Dashboard.tsx</files>
  <action>
Restructure the Dashboard page to have three distinct workflow sections:

1. **Capture Section** (top on mobile, left on desktop):
   - Background: warm/amber tint (bg-amber-50/5)
   - Header: "Capture" with brief description
   - Contains: QuickCaptureCard (prominent, large), location presets if available
   - Always visible, not collapsible

2. **Review Section** (middle on mobile, right on desktop):
   - Background: cool/teal tint (bg-teal-50/5)
   - Header: "Review" with pending badge
   - Contains: Review link, recent inspections preview
   - Always visible, not collapsible

3. **Analyze Section** (bottom):
   - Background: neutral (bg-slate-50)
   - Header: "Analyze"
   - Contains: Stats summary, analytics links
   - Collapsible on mobile (optional enhancement)

Layout approach:
- Mobile (< sm): Single column, stacked sections
- Desktop (>= sm): Two-column grid (Capture | Review), Analyze spans both columns below

Apply safe area insets to bottom elements using pb-[env(safe-area-inset-bottom)].

Keep existing FAB from Phase 04 (it provides universal scroll-aware access).

Import QuickCaptureCard and place it prominently in the Capture section.
  </action>
  <verify>grep -n "QuickCaptureCard\|Capture\|Review\|Analyze" src/pages/Dashboard.tsx | head -20</verify>
  <done>Dashboard has three workflow sections with background color blocks, QuickCaptureCard is imported and rendered in Capture section. Layout uses responsive grid.</done>
</task>

<task type="auto">
  <name>Wire QuickCaptureCard to navigation</name>
  <files>src/pages/Dashboard.tsx</files>
  <action>
Import useNavigate from react-router-dom in Dashboard.tsx.

Add onClick handler to QuickCaptureCard:
```typescript
const navigate = useNavigate();

<QuickCaptureCard 
  onClick={() => navigate('/quick-capture')}
/>
```

Verify the quick-capture route exists in App.tsx (it should from Phase 03-04).

Test that clicking the card navigates to Quick Capture page.
  </action>
  <verify>grep -n "navigate.*quick-capture\|QuickCaptureCard.*onClick" src/pages/Dashboard.tsx</verify>
  <done>QuickCaptureCard onClick handler navigates to /quick-capture route. No TypeScript errors.</done>
</task>

</tasks>

<verification>
- [ ] QuickCaptureCard component exists with icon, label, description, amber styling
- [ ] Dashboard has three workflow sections with distinct background colors
- [ ] Quick Capture section is most prominent (amber background, top of visual hierarchy)
- [ ] QuickCaptureCard positioned in bottom 1/3 on mobile (thumb zone)
- [ ] Primary actions are always visible (not collapsed behind accordions)
- [ ] Clicking QuickCaptureCard navigates to /quick-capture
- [ ] npm run check passes (TypeScript)
- [ ] npm run build succeeds
</verification>

<success_criteria>
1. Dashboard displays three workflow sections (Capture, Review, Analyze) with background color separation
2. Quick Capture is the most visually prominent element in the bottom 1/3 of mobile viewport
3. QuickCaptureCard shows camera icon + "Quick Capture" label + "Capture issues while walking" description
4. Card uses amber/warm color theme consistent with capture workflow
5. Card has minimum 120px touch target height for gloved hands
6. Card click navigates to quick-capture page
7. Layout is responsive (mobile: stacked, desktop: side-by-side Capture/Review)
</success_criteria>

<output>
After completion, create `.planning/phases/12-home-page-layout-reorganization/12-01-SUMMARY.md`
</output>
